
import { PlantDetails } from "../model/PlantDetails"

export const PlantDetailScreen = (id: number) => {

      // Handlers
  const handleEdits = (event) => {
    // update plant's details manually
  }


  // Actions
const handleUpdate = (event) => {
  // Update the plant's details, potentially by searching the parentual API in a modal
} 

    // Fetch this from cache or API
    return (
        <div>
            <h1>Plant Details</h1>
        </div>
    )
}